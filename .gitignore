node_modules/
*.log

# NOTE: dist/ is intentionally NOT ignored  - JavaScript actions must commit
# their bundled dist/index.js so `uses: <owner>/<repo>` works without a build
# step in the consumer workflow.
